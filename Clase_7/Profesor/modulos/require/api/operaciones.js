const suma = (a,b) => a + b
const resta = (a,b) => a - b
const mult = (a,b) => a * b
const div = (a,b) => a / b

module.exports = {
    suma,   // es igual a -> suma : suma,
    resta,
    mult,
    div,
}